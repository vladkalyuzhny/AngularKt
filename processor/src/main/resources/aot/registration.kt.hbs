package at.angular.generated

/**
 * Generated by AngularKt KSP processor (aot mode) — do not edit.
 *
 * A no-op counterpart to the JIT manifest: in AOT the decorators are already baked into the
 * compiled `.ts` bridges, so there is nothing to register at runtime. It exists only so a shared
 * entry point that calls `registerAngularKt()` (the JIT `main()`) still compiles in the AOT
 * library build, where that call is dead code.
 */
fun registerAngularKt() { /* no-op: AOT registration runs through the compiled TS bridges */ }
