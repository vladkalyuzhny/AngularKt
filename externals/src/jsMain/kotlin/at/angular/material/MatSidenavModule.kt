// Hand-added to mirror Karakum's generated Material module externals (@angular/material/sidenav).
// Only the `@file:JsModule` + `external class` are load-bearing: the Gradle plugin's externals scan
// (NgExternalsScan) maps this FQN to its npm module for the AOT processor, and importing the module
// in an @NgModule pulls in mat-sidenav-container / mat-sidenav / mat-sidenav-content.

@file:JsModule("@angular/material/sidenav")

package at.angular.material

external class MatSidenavModule {

companion object {



}
}
